// main_run.txt — central loader
logToPanel("main_run.txt loaded");

// Define bot functions globally
window.initbot_run = function(cycles, user) {
    logToPanel("[InitBot] Simulation started for " + cycles + " cycles for " + user);
    // … trading loop logic …
};

window.stopbot_run = function(user) {
    logToPanel("[StopBot] Stopping bot for " + user);
    // … cleanup logic …
};

window.deposit_run = function(user) {
    logToPanel("[Deposit] Depositing funds for " + user);
    // … deposit logic …
};

window.withdraw_run = function(user) {
    logToPanel("[Withdraw] Withdrawing funds for " + user);
    // … withdraw logic …
};

// Wire UI after DOM is ready
document.addEventListener("DOMContentLoaded", () => {
    logToPanel("UI ready");

    const startBtn = document.getElementById("startBtn");
    if (startBtn) {
        startBtn.addEventListener("click", () => {
            const user = "mockuser1";
            initbot_run(10, user);
        });
    }

    const stopBtn = document.getElementById("stopBtn");
    if (stopBtn) {
        stopBtn.addEventListener("click", () => {
            const user = "mockuser1";
            stopbot_run(user);
        });
    }

    const depositBtn = document.getElementById("depositBtn");
    if (depositBtn) {
        depositBtn.addEventListener("click", () => {
            const user = "mockuser1";
            deposit_run(user);
        });
    }

    const withdrawBtn = document.getElementById("withdrawBtn");
    if (withdrawBtn) {
        withdrawBtn.addEventListener("click", () => {
            const user = "mockuser1";
            withdraw_run(user);
        });
    }
});
