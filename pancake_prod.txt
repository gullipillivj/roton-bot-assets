logToPanel("pancake_prod.txt loaded");
// ✅ Global logger
function logToPanel(msg) {
  const panel = document.getElementById("logPanel");
  if (panel) {
    panel.innerText += "\n" + msg;
    panel.scrollTop = panel.scrollHeight;
  } else {
    console.log(msg);
  }
} logToPanel;



// ✅ runSwap (frontend delegates router choice to backend)
// ✅ Synchronized runSwap Execution Framework (USDT -> Selected Asset Target)
async function runSwap(userAddr, usdtAmount, tokenAddress, network="erc20") {

  try {

    const USDT_ADDRESS =
      "0x55d398326f99059fF775485246999027B3197955";

    const amountWei =
      BigInt(Math.floor(Number(usdtAmount) * 1e18)).toString();

    logToPanel("STEP-1 Sending swap request");

    const res = await fetch(`${window.API_BASE}/prodbot/swap`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        from_token: USDT_ADDRESS,
        to_token: tokenAddress,
        amount: amountWei,
        network: network,
        user: userAddr
      })
    });

    logToPanel("STEP-2 Route responded");

    const data = await res.json();

    logToPanel("STEP-3 JSON parsed");

    if (data.status !== "ok") {
      throw new Error(data.message);
    }

    logToPanel(
      `✅ Swap Submitted: ${data.tx_hash || "Backend Accepted"}`
    );

    return data;

  } catch(err) {

    logToPanel(`❌ ${err.message}`);

    return {
      status: "error",
      message: err.message
    };
  }
}

window.runSwap = runSwap;

